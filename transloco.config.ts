import {TranslocoGlobalConfig} from '@jsverse/transloco-utils';

const config: TranslocoGlobalConfig = {
  rootTranslationsPath: 'src/assets/i18n/',
  langs: [ 'en', 'nl', 'fr', 'de', 'es', 'it', 'pl' ],
  keysManager: {}
};

export default config;
